{
  "_comment": "Synthetic MCP config used by `bumblebee selftest`. The image ref is deliberately implausible so this fixture cannot collide with a real catalog entry.",
  "mcpServers": {
    "bumblebee-selftest-mcp": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "ghcr.io/bumblebee-selftest/evil-mcp:0.0.0"
      ]
    }
  }
}
